"use client";

export function LevelEditor() {
  return (
    <div className="border border-hairline bg-surface p-4 text-sm text-muted">
      The grid level editor is retired for the bridge playtest. Room content now lives in JSON.
    </div>
  );
}
